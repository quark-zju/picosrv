# Fail2ban filter for picosrv requests whose policy classifies the observed
# behavior as sufficient evidence for an immediate temporary ban.

[Definition]
failregex = ^.*"client_ip":"<HOST>".*"decision_reason":"likely_abuse".*"ban_candidate":true.*$
ignoreregex =

[Init]
journalmatch = _SYSTEMD_UNIT=picosrv.service
